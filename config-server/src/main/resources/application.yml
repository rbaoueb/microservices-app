server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rbaoueb/config-repo
          clone-on-start: true
          default-label: main # ou master selon votre branche
